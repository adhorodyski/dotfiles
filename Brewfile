brew 'fnm'
brew 'gh'
brew 'git'
brew 'git-delta'
brew 'gnupg'
brew 'neovim'
brew 'ripgrep'
brew 'tree'
brew 'worktrunk'

# macOS-only (RN/iOS toolchain + GUI terminal). Skipped on Linux by brew bundle.
if OS.mac?
  brew 'cocoapods'
  brew 'rbenv'
  brew 'watchman'
  cask 'ghostty'
end
