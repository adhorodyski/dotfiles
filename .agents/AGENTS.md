# AGENTS.md

- **Link GitHub issues and PRs.** Write each one as a markdown link whose label is the full `owner/repo#number` slug: `[acme/widgets#123](https://github.com/acme/widgets/issues/123)`, `/pull/` for PRs. A bare `#123` is unclickable and drops the repo.
