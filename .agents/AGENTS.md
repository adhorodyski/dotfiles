# AGENTS.md

## Rules

1. **Never break userspace.** Working product behavior is sacred - UI flows, API contracts, anything users touch. If a change risks breaking it, stop and surface the tradeoff.

## Git

When working with worktrees, always use the `wt` CLI (worktrunk).
